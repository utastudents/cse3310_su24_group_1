package uta.cse3310;

public class Gameplay {
   
    private String validWords;
    private String userGuess;
    private String madeGuesses;
    private Player player;

    PlayerType Players;

    // Methods
    public void playerInputGuess(player, User_Input) //input is the player and the input
    {
        /*Pseudo code 
        userGuess = player.User_Input();
        private string madeGuesses = userguesses 
        */
    }

public boolean answerCheck(String userGuess) {
    public displayResponse()
    {
        /*
         * if userGuess == correct character in the phrase
         * userScore++;
         * display.guesscorrect();
         * else if (purchaseVowel)
         * display a vowel
         * else
         * display.guessincorrect();
         * skip
         */
    }
}

   
    public void switchPlayer(Players) {
        /*
         * change player value to the next one
         * so player = player[1];
         */
    }

    public void purchaseVowel(Players, playerBankAccount)
    {
        /*
         * if player pushes button to purchase a vowel
         * answer check will display a correct character in the phrase that is a vowel
         * deduct the amount from the playerBankAccount
         */
    }

    public void userStake(Players, playerInventory)
    {
        /*
         * 
         */
    }
}
